<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>تعديل الفاتورة</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 40px;
      background-color: #f9f9f9;
    }
    h2 {
      color: #2c3e50;
      margin-bottom: 20px;
    }
    form {
      background: #fff;
      padding: 25px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.05);
      max-width: 600px;
      margin: auto;
    }
    label {
      display: block;
      margin-bottom: 6px;
      font-weight: bold;
    }
    input {
      width: 100%;
      padding: 10px;
      margin-bottom: 16px;
      border: 1px solid #ccc;
      border-radius: 4px;
    }
    button {
      background-color: #27ae60;
      color: white;
      padding: 12px 20px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }
    button:hover {
      background-color: #219150;
    }
  </style>
</head>
<body>

  <h2>تعديل بيانات الفاتورة للعميل: <%= booking.client_id?.name || "غير معروف" %></h2>

  <form action="/generate-invoice-preview" method="POST">
    <input type="hidden" name="bookingId" value="<%= booking._id %>">

    <label>سعر الإيجار (Office Rent):</label>
    <input type="number" step="0.01" name="total_price" value="<%= booking.total_price %>" required />

    <label>الرسوم الإدارية (Admin Fee):</label>
    <input type="number" step="0.01" name="admin_fee" value="<%= booking.admin_fee %>" required />

    <label>العمولة (Commission):</label>
    <input type="number" step="0.01" name="commission" value="<%= booking.commission %>" required />

    <label>مبلغ التأمين (Security Deposit):</label>
    <input type="number" step="0.01" name="sec_deposit" value="<%= booking.sec_deposit %>" required />

    <button type="submit">📄 عرض الفاتورة</button>
  </form>

</body>
</html>
