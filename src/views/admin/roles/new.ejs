<%- include('../../partials/header') %>

<div class="container py-5">
  <h2 class="mb-4 text-primary">➕ إنشاء دور جديد</h2>

  <!-- رسالة خطأ لو في -->
  <% if (typeof error !== 'undefined') { %>
    <div class="alert alert-danger"><%= error %></div>
  <% } %>

  <!-- فورم إنشاء الدور -->
  <form method="POST" action="/admin/roles">
    <div class="mb-3">
      <label class="form-label">اسم الدور</label>
      <input type="text" name="name" class="form-control" placeholder="مثلاً: مشرف - مدير النظام" required>
    </div>

    <div class="mb-3">
      <label class="form-label">الصلاحيات</label>
      <div class="row">
        <% permissions.forEach(permission => { %>
          <div class="col-md-4">
            <div class="form-check">
              <input class="form-check-input" type="checkbox" name="permissions" value="<%= permission._id %>" id="perm_<%= permission._id %>">
              <label class="form-check-label" for="perm_<%= permission._id %>">
                <%= permission.key %>
              </label>
            </div>
          </div>
        <% }) %>
      </div>
    </div>

    <button type="submit" class="btn btn-primary">💾 حفظ الدور</button>
  </form>

  <hr class="my-5" />

  <!-- جدول عرض الأدوار الموجودة -->
  <h3 class="text-secondary mb-3">🧾 الأدوار الحالية</h3>
  <table class="table table-bordered bg-white shadow-sm">
    <thead class="table-light">
      <tr>
        <th>#</th>
        <th>الاسم</th>
        <th>الصلاحيات</th>
      </tr>
    </thead>
    <tbody>
      <% roles.forEach((role, index) => { %>
        <tr>
          <td><%= index + 1 %></td>
          <td><span class="badge bg-dark text-white"><%= role.name %></span></td>
          <td>
            <% if (role.permissions?.length) { %>
              <% role.permissions.forEach(p => { %>
                <span class="badge bg-info text-dark mb-1"><%= p.key %></span>
              <% }) %>
            <% } else { %>
              <span class="text-muted">لا يوجد صلاحيات</span>
            <% } %>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

<%- include('../../partials/footer') %>
